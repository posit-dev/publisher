{
  "unit-vitest": {
    "runnerCommand": "vitest run"
  },
  "e2e-cypress": {
    "runnerCommand": "cross-env NODE_ENV=test start-test \"npx quasar dev\" http-get://google.com \"cypress run --e2e\""
  },
  "component-cypress": {
    "runnerCommand": "cross-env NODE_ENV=test cypress run --component"
  }
}
