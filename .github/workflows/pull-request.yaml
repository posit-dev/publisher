name: Pull Request
on:
  pull_request:
  push:
    branches:
      - tdstein/release
jobs:
  agent:
    uses: ./.github/workflows/agent.yaml
  web:
    uses: ./.github/workflows/web.yaml
  build:
    uses: ./.github/workflows/build.yaml
