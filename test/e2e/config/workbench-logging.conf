# RStudio Workbench logging configuration
# Configures server logs to go to files in the mapped volume

[*]
logger-type=file
log-level=info
log-dir=/var/log/rstudio/rstudio-server

# Launcher logging configuration
[rserver-launcher]
logger-type=file
log-level=info
log-dir=/var/log/rstudio/launcher