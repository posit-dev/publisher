{
  "name": "extension-contract-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check:conformance": "tsc --noEmit -p tsconfig.conformance.json"
  },
  "devDependencies": {
    "@types/vscode": "^1.87.0",
    "typescript": "^5.7.0",
    "vitest": "^4.1.0"
  }
}
