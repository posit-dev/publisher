{
  "name": "extension-contract-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "check:conformance": "tsc --noEmit -p tsconfig.conformance.json"
  },
  "dependencies": {
    "@posit-dev/connect-cloud-api": "*"
  },
  "devDependencies": {
    "@posit-dev/positron": "^0.2.10",
    "@types/vscode": "1.105.0",
    "js-yaml": "^5.4.2",
    "typescript": "^7.0.2",
    "vitest": "^5.0.1"
  }
}
