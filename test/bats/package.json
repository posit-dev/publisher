{
  "private": true,
  "scripts": {
    "deploy": "bats -r -T --formatter junit -o ../results ./contract/deploy.bats",
    "accounts": "bats -r -T --formatter junit -o ../results ./contract/accounts.bats",
    "common": "bats -r -T --formatter junit -o ../results ./cli/common.bats",
    "init": "bats -r -T --formatter junit -o ../results ./cli/init.bats"
  },
  "devDependencies": {
    "bats": "^1.10.0",
    "bats-assert": "github:ztombol/bats-assert",
    "bats-support": "github:ztombol/bats-support"
  }
}
