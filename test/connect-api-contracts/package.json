{
  "name": "connect-api-contracts",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:update": "vitest run --update",
    "validate-fixtures": "vitest run --config vitest.validate.config.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@posit-dev/connect-api": "*",
    "axios": "^1.15.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "ajv": "^8.20.0",
    "ajv-formats": "^3.0.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.4"
  }
}
