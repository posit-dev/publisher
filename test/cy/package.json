{
  "private": true,
  "scripts": {
    "lint": "eslint --ext .js,.ts,.cjs .",
    "fix": "eslint --ext .js,.ts,.cjs . --fix",
    "start": "bash -c 'just ../../run init ./test/sample-content/fastapi-simple && just ../../run publish-ui --listen 127.0.0.1:9000 ./test/sample-content/fastapi-simple'",
    "test": "start-server-and-test --expect 200 start http-get://127.0.0.1:9000 run",
    "open": "cypress open",
    "run": "cypress run"
  },
  "devDependencies": {
    "cypress": "^13.3.0",
    "eslint": "^8.51.0",
    "eslint-plugin-cypress": "^2.15.1",
    "start-server-and-test": "^2.0.1"
  }
}
