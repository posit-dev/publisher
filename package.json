{
  "name": "publishing-client",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "check-format": "prettier . --check",
    "prepare": "husky"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "devDependencies": {
    "husky": "^9.0.11",
    "lint-staged": "^16.2.6",
    "prettier": "^3.4.2"
  }
}
