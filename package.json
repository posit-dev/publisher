{
  "name": "publishing-client",
  "private": true,
  "scripts": {
    "format": "prettier . --write",
    "check-format": "prettier . --check"
  },
  "devDependencies": {
    "prettier": "3.2.5"
  }
}
