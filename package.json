{
  "name": "publishing-client",
  "private": true,
  "type": "module",
  "scripts": {
    "format": "prettier . --write",
    "check-format": "prettier . --check",
    "prepare": "husky"
  },
  "lint-staged": {
    "*": [
      "prettier --write --ignore-unknown"
    ]
  },
  "devDependencies": {
    "@types/node": "^25.3.5",
    "husky": "^9.0.11",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1"
  }
}
